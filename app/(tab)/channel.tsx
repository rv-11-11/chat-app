import { Text, View } from 'react-native';
import "../../global.css";

const Channel = () => { 
    return (
      <View className="flex-1 justify-center items-center bg-white">
        <Text className="text-2xl font-bold">Channel Screen</Text>
      </View>
    );
}
export default Channel;